extends CombatRobotMainBattleElite

## An intentionally unknown derived implementation. Production capability
## checks must fail closed so new subclasses cannot silently inherit a phase
## split that has not passed their own semantic gate.
