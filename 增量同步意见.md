# 增量同步意见

## 当前结构判断

- 玩家输入、玩家/敌人状态、投射物/技能事件、可靠事件已经分在不同 ENet channel，结构清晰，当前流畅度的基础是可靠的。
- `SnapshotManager` 已经实现了玩家和敌人快照的变化掩码，也保留了 `_prev_player_states` / `_prev_enemy_states` 缓存。
- 但当前 `encode_all_player_snapshots()` 和 `encode_all_enemy_snapshots()` 调用单体编码时仍传入 `previous = null`，因此每个实体每次都会写入完整字段。现在实际运行的是“量化后的全量快照”，还没有真正使用增量缓存。
- 当前频率是玩家 60Hz、敌人 30Hz，配合 int16 量化位置/速度，对现有战局足够顺滑；战局更复杂时，瓶颈会优先出现在敌人数量、投射物事件突发和可靠事件排队。

## 建议的增量同步路线

1. 先做 per-peer 增量缓存

每个接收端维护独立的上一帧发送状态，而不是全局上一帧。原因是客户端可能丢包、晚加入、重连，不能假设所有 peer 的基准状态相同。

建议结构：

- `player_delta_baseline_by_peer[peer_id][player_id]`
- `enemy_delta_baseline_by_peer[peer_id][net_id]`
- 新玩家加入、场景切换、波次重置时清空该 peer 的 baseline。

2. 加入周期性 keyframe

增量包依赖基准状态，unreliable channel 丢包后可能短时间缺字段。建议：

- 每 0.5 到 1 秒发送一次全量 keyframe。
- 关键实体生成、复活、死亡、换形态、Boss 阶段切换时强制全量。
- 客户端检测序号跳变过大时请求一次全量状态恢复。

3. 保持可靠事件和状态快照分工

不要把伤害、死亡、复活、掉落、升级购买这些确定性结果改成 unreliable 增量。它们现在放在 reliable event channel 是合理的。

增量同步适合：

- 位置、速度、朝向。
- 敌人血量的快照兜底。
- 动画状态或短 action id 的重复容错。

可靠事件适合：

- 玩家受到伤害。
- 玩家死亡/复活。
- 敌人生成/移除。
- 掉落生成/拾取。
- 商店、升级、技能解锁。

4. 对敌人做兴趣管理和自适应频率

敌人数量上来后，不建议所有敌人固定 30Hz。

优先级建议：

- 距任意玩家近、正在攻击、Boss、精英敌人：30Hz。
- 中距离追踪敌人：15Hz。
- 远距离且稳定移动敌人：8 到 10Hz。
- 离所有玩家很远且不可见敌人：只发低频位置 keyframe 和可靠事件。

这样比单纯提高带宽上限更稳，也能支持更复杂战局。

5. 对大包做预算和拆包

当前快照包如果随着敌人数量增大会接近 MTU。建议增加运行时统计：

- 每个 channel 每秒字节数。
- 每个 peer 的快照包大小移动平均。
- 玩家快照、敌人快照、投射物事件分别统计。
- 超过预算时优先降远端敌人频率，而不是让单包继续膨胀。

敌人快照超过预算时，可以按优先级拆成多包，并保证高优先级实体先发。

6. 动画同步保持事件化

动画不建议塞进每帧状态里做高频同步。当前新增/已有的 `broadcast_enemy_action` 路线更合适：

- 移动/待机动画由快照速度和本地代理状态推导。
- 攻击、锁定、蓄力、爆炸等离散动作通过 action event 发送。
- action event 带递增 id，客户端丢弃旧动作。
- 长持续视觉效果需要在代理端按目标实时更新，例如狙击手锁定线。

7. 投射物事件保留轻量化

投射物/技能事件通常是突发流量。建议保持事件包小而明确：

- 只传生成所需的 source id、位置、方向、配置 id、事件 id。
- 命中仍由 Host 权威确认。
- 对视觉-only 的本地提示不进网络，例如玩家自己的复活光圈。

## 暂不建议现在删除的代码

- `SnapshotManager` 的 `_prev_player_states` / `_prev_enemy_states` 当前没有发挥增量作用，但它们正好是后续增量同步的入口，不建议删除。
- `reset_delta_cache()` 目前价值有限，但增量同步启用后会用于重连、换场景、重新开始游戏，不建议删除。
- `relay_servers` 下的 stub 是 relay Godot 工程的运行面，不是主项目废弃代码。
- `dev_tools` 下的 probe/smoke 脚本仍然覆盖多人真实连接、掉线、波次、Boss、复活等路径，不建议作为历史产物清理。

## 推荐执行顺序

1. 给 `SnapshotManager` 批量编码接入 per-peer previous state，并保留周期性 keyframe。
2. 增加快照包大小和 channel 字节数统计，先观察真实战局峰值。
3. 对敌人快照加入优先级和动态频率。
4. 在压力场景稳定后，再考虑投射物事件聚合或压缩。
